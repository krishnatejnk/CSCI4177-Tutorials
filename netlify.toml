# Netlify config for Tutorial 1 deployment (static HTML)
# Publish directory: tutorial1 (static HTML, no build)
#
# For Tutorial 3: Create a separate Netlify site, set Base directory = "tutorial3",
# Build command = "npm run build", Publish directory = "tutorial3/dist"
[build]
  publish = "tutorial1"
